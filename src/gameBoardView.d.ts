import { XAxisWords, YAxisWords } from "./model";
export type GameBoardView = {
    renderAxes: (xwords: XAxisWords, ywords: YAxisWords) => void;
};
declare const _default: GameBoardView;
export default _default;
